generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "debian-openssl-3.0.x"]
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

enum Role {
  SUPER_ADMIN
  TENANT_ADMIN
  TECNICO_SST
  GESTOR
  RH
  CONSULTA
}

enum TenantStatus {
  ACTIVE
  SUSPENDED
  TRIAL
  CANCELED
}

enum NotificationType {
  INFO
  SUCCESS
  WARNING
  ERROR
}

enum AuditAction {
  CREATE
  UPDATE
  DELETE
  LOGIN
  LOGIN_FAILED
  LOGIN_MFA_REQUEST
  LOGIN_MFA_COMPLETE
  LOGIN_MFA_FAILED
  LOGOUT
  REFRESH
  PASSWORD_RESET_REQUEST
  PASSWORD_RESET_COMPLETE
  USER_PERMISSION_CHANGED
  USER_ACTIVATED
  USER_DEACTIVATED
  TENANT_STATUS_CHANGED
  ACTIVATE
  DEACTIVATE
}

enum EmployeeGender {
  MALE
  FEMALE
  OTHER
}

enum EpiDeliveryReason {
  ADMISSION
  PERIODIC
  REPLACEMENT
  FUNCTION_CHANGE
  CA_RENEWAL
  OTHER
}

enum EpiCondition {
  NEW
  USED
}

enum OccupationalExamType {
  ADMISSIONAL
  PERIODIC
  RETURN_TO_WORK
  ROLE_CHANGE
  DISMISSAL
  COMPLEMENTARY
}

enum OccupationalExamResult {
  FIT
  UNFIT
  FIT_WITH_RESTRICTIONS
  PENDING
}

enum TrainingStatus {
  SCHEDULED
  COMPLETED
  EXPIRED
  CANCELED
}

enum FunctionStatus {
  ACTIVE
  INACTIVE
}

enum RiskType {
  PHYSICAL
  CHEMICAL
  BIOLOGICAL
  ERGONOMIC
  ACCIDENT
}

enum RiskLevel {
  NEGLIGIBLE
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum RiskProbability {
  RARE
  UNLIKELY
  POSSIBLE
  LIKELY
  ALMOST_CERTAIN
}

enum RiskSeverity {
  INSIGNIFICANT
  MINOR
  MODERATE
  MAJOR
  CATASTROPHIC
}

enum AccidentStatus {
  REPORTED
  UNDER_INVESTIGATION
  ACTION_PLAN_DEFINED
  CLOSED
}

enum AccidentSeverity {
  MINOR
  MODERATE
  SERIOUS
  FATAL
}

enum AccidentType {
  TYPICAL
  COMMUTE
  OCCUPATIONAL_DISEASE
  NEAR_MISS
}

enum AccidentActivityType {
  HABITUAL
  EVENTUAL
  NEW
  FUNCTION_REPLACEMENT
}

enum AccidentTypicalSubtype {
  BATIDA_CONTRA
  QUEDA_MESMO_NIVEL
  QUEDA_NIVEL_DIFERENTE
  PERFUROCORTANTE
  PRENSAMENTO
  AGRESSAO
  QUEIMADURA
  CONTATO_MATERIAL_BIOLOGICO
  CONTATO_PRODUTO_QUIMICO
  OTHER
}

enum AccidentCommuteSubtype {
  BATIDA_CONTRA
  COLISAO_VEICULOS
  ATROPELAMENTO
  QUEDA_NIVEL
  ASSALTO
  AGRESSAO
  ATAQUE_ANIMAIS
  OTHER
}

enum AccidentWorkJourneyType {
  REGULAR_HOURS
  OVERTIME
  CHANGED_SCHEDULE
}

enum AccidentInjuredSide {
  LEFT
  RIGHT
  BOTH
}

enum AccidentBodyPart {
  HEAD
  FACE
  EYE
  NOSE
  MOUTH
  NECK
  SHOULDER
  ARM
  ELBOW
  WRIST
  HAND
  FINGER
  THORAX
  ABDOMEN
  BACK
  COCCYX
  THIGH
  LEG
  KNEE
  ANKLE
  FOOT
  OTHER
}

enum AccidentEvidenceType {
  INJURY_PHOTO
  PROCEDURE_EXECUTED
  PPE_CORRECT_USE
  POLICE_REPORT
  MEDICAL_CERTIFICATE_WITH_CID
  MEDICAL_ATTENDANCE_DECLARATION
  OTHER
}

enum GeneratedDocumentStatus {
  ACTIVE
  DELETED
}

enum SstLegalDocumentType {
  PGR
  PCMSO
  LTCAT
  LIP
}

enum SstLegalDocumentStatus {
  DRAFT
  ACTIVE
  SUPERSEDED
  ARCHIVED
}

enum DocumentAuditAction {
  CREATED
  DELETED
}

model Tenant {
  id        String       @id @default(uuid())
  name      String
  slug      String       @unique
  status    TenantStatus @default(TRIAL)
  plan      String       @default("manual")
  startDate DateTime?    @map("start_date") @db.Date
  endDate   DateTime?    @map("end_date") @db.Date
  isActive  Boolean      @default(false) @map("is_active")
  createdAt DateTime     @default(now()) @map("created_at")
  updatedAt DateTime     @updatedAt @map("updated_at")

  companies          Company[]
  units              Unit[]
  sectors            Sector[]
  jobFunctions       JobFunction[]
  functionUnits      FunctionUnit[]
  risks              Risk[]
  accidents          Accident[]
  functionTemplates  FunctionTemplate[]
  generatedDocuments GeneratedDocument[]
  accidentTemplates  AccidentTemplate[]
  accidentGeneratedDocuments AccidentGeneratedDocument[]
  sstLegalDocuments  SstLegalDocument[]
  accidentEvidences  AccidentEvidence[]
  documentAuditLogs  DocumentAuditLog[]
  employees          Employee[]
  epiItems           EpiItem[]
  epiDeliveries      EpiDelivery[]
  occupationalExams  OccupationalExam[]
  trainings          Training[]
  users              User[]
  notifications      Notification[]
  auditLogs          AuditLog[]

  @@index([status, isActive])
  @@map("tenants")
}

model Company {
  id                  String    @id @default(uuid())
  tenantId            String    @map("tenant_id")
  name                String
  tradeName           String?   @map("trade_name")
  cnpj                String
  cnae                String?
  addressStreet       String?   @map("address_street")
  addressNumber       String?   @map("address_number")
  addressComplement   String?   @map("address_complement")
  addressNeighborhood String?   @map("address_neighborhood")
  addressCity         String?   @map("address_city")
  addressState        String?   @map("address_state")
  addressZipCode      String?   @map("address_zip_code")
  phone               String?
  email               String?
  responsibleName     String?   @map("responsible_name")
  responsibleCpf      String?   @map("responsible_cpf")
  logoUrl             String?   @map("logo_url")
  isActive            Boolean   @default(true) @map("is_active")
  createdAt           DateTime  @default(now()) @map("created_at")
  updatedAt           DateTime  @updatedAt @map("updated_at")
  deletedAt           DateTime? @map("deleted_at")
  deletedBy           String?   @map("deleted_by")

  tenant        Tenant              @relation(fields: [tenantId], references: [id])
  units         Unit[]
  primaryUsers  User[]              @relation("PrimaryCompany")
  userAccess    UserCompanyAccess[]
  sectors       Sector[]
  jobFunctions  JobFunction[]
  risks         Risk[]
  accidents     Accident[]
  accidentTemplates AccidentTemplate[]
  accidentGeneratedDocuments AccidentGeneratedDocument[]
  sstLegalDocuments SstLegalDocument[]
  accidentEvidences AccidentEvidence[]
  employees     Employee[]
  epiItems      EpiItem[]
  epiDeliveries EpiDelivery[]
  occupationalExams OccupationalExam[]
  trainings     Training[]
  notifications Notification[]

  @@unique([tenantId, cnpj])
  @@index([tenantId])
  @@index([tenantId, deletedAt])
  @@map("companies")
}

model Unit {
  id                  String    @id @default(uuid())
  tenantId            String    @map("tenant_id")
  companyId           String    @map("company_id")
  name                String
  cnpj                String?
  addressStreet       String?   @map("address_street")
  addressNumber       String?   @map("address_number")
  addressComplement   String?   @map("address_complement")
  addressNeighborhood String?   @map("address_neighborhood")
  addressCity         String?   @map("address_city")
  addressState        String?   @map("address_state")
  addressZipCode      String?   @map("address_zip_code")
  phone               String?
  email               String?
  isActive            Boolean   @default(true) @map("is_active")
  createdAt           DateTime  @default(now()) @map("created_at")
  updatedAt           DateTime  @updatedAt @map("updated_at")
  deletedAt           DateTime? @map("deleted_at")
  deletedBy           String?   @map("deleted_by")

  tenant             Tenant              @relation(fields: [tenantId], references: [id])
  company            Company             @relation(fields: [companyId], references: [id])
  sectors            Sector[]
  jobFunctions       JobFunction[]
  risks              Risk[]
  accidents          Accident[]
  accidentGeneratedDocuments AccidentGeneratedDocument[]
  employees          Employee[]
  userAccess         UserUnitAccess[]
  functionUnits      FunctionUnit[]
  generatedDocuments GeneratedDocument[]
  sstLegalDocuments  SstLegalDocument[]
  occupationalExams  OccupationalExam[]
  trainings          Training[]

  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([companyId])
  @@map("units")
}

model Sector {
  id          String    @id @default(uuid())
  tenantId    String    @map("tenant_id")
  companyId   String    @map("company_id")
  unitId      String    @map("unit_id")
  name        String
  description String?
  isActive    Boolean   @default(true) @map("is_active")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")
  deletedAt   DateTime? @map("deleted_at")
  deletedBy   String?   @map("deleted_by")

  tenant       Tenant        @relation(fields: [tenantId], references: [id])
  company      Company       @relation(fields: [companyId], references: [id])
  unit         Unit          @relation(fields: [unitId], references: [id])
  jobFunctions JobFunction[]
  employees    Employee[]

  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([unitId])
  @@map("sectors")
}

model JobFunction {
  id          String         @id @default(uuid())
  tenantId    String         @map("tenant_id")
  companyId   String         @map("company_id")
  unitId      String?        @map("unit_id")
  sectorId    String?        @map("sector_id")
  name        String
  cbo         String?
  description String?
  status      FunctionStatus @default(ACTIVE)
  isActive    Boolean        @default(true) @map("is_active")
  createdAt   DateTime       @default(now()) @map("created_at")
  updatedAt   DateTime       @updatedAt @map("updated_at")
  deletedAt   DateTime?      @map("deleted_at")
  deletedBy   String?        @map("deleted_by")

  tenant             Tenant              @relation(fields: [tenantId], references: [id])
  company            Company             @relation(fields: [companyId], references: [id])
  unit               Unit?               @relation(fields: [unitId], references: [id])
  sector             Sector?             @relation(fields: [sectorId], references: [id])
  employees          Employee[]
  functionUnits      FunctionUnit[]
  risks              Risk[]
  accidents          Accident[]
  occupationalExams  OccupationalExam[]
  trainings          Training[]
  templates          FunctionTemplate[]
  generatedDocuments GeneratedDocument[]

  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([unitId])
  @@index([tenantId, status])
  @@map("job_functions")
}

model FunctionUnit {
  id            String   @id @default(uuid())
  tenantId      String   @map("tenant_id")
  jobFunctionId String   @map("function_id")
  unitId        String   @map("unit_id")
  createdAt     DateTime @default(now()) @map("created_at")

  tenant      Tenant      @relation(fields: [tenantId], references: [id])
  jobFunction JobFunction @relation(fields: [jobFunctionId], references: [id], onDelete: Cascade)
  unit        Unit        @relation(fields: [unitId], references: [id], onDelete: Cascade)

  @@unique([jobFunctionId, unitId])
  @@index([tenantId])
  @@index([unitId])
  @@map("function_units")
}

model Employee {
  id            String          @id @default(uuid())
  tenantId      String          @map("tenant_id")
  companyId     String          @map("company_id")
  unitId        String          @map("unit_id")
  sectorId      String?         @map("sector_id")
  jobFunctionId String          @map("job_function_id")
  name          String
  cpf           String
  email         String?
  phone         String?
  birthDate     DateTime?       @map("birth_date") @db.Date
  gender        EmployeeGender?
  registration  String?
  admissionDate DateTime        @map("admission_date") @db.Date
  dismissalDate DateTime?       @map("dismissal_date") @db.Date
  isActive      Boolean         @default(true) @map("is_active")
  createdAt     DateTime        @default(now()) @map("created_at")
  updatedAt     DateTime        @updatedAt @map("updated_at")
  deletedAt     DateTime?       @map("deleted_at")
  deletedBy     String?         @map("deleted_by")

  tenant             Tenant              @relation(fields: [tenantId], references: [id])
  company            Company             @relation(fields: [companyId], references: [id])
  unit               Unit                @relation(fields: [unitId], references: [id])
  sector             Sector?             @relation(fields: [sectorId], references: [id])
  jobFunction        JobFunction         @relation(fields: [jobFunctionId], references: [id])
  accidents          Accident[]
  epiDeliveries      EpiDelivery[]
  occupationalExams  OccupationalExam[]
  trainings          Training[]
  generatedDocuments GeneratedDocument[]
  accidentGeneratedDocuments AccidentGeneratedDocument[]

  @@unique([tenantId, companyId, cpf])
  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([unitId])
  @@map("employees")
}

model Risk {
  id              String          @id @default(uuid())
  tenantId        String          @map("tenant_id")
  companyId       String          @map("company_id")
  unitId          String          @map("unit_id")
  jobFunctionId   String?         @map("job_function_id")
  name            String
  type            RiskType
  level           RiskLevel
  probability     RiskProbability
  severity        RiskSeverity
  description     String?
  controlMeasures String?         @map("control_measures")
  isActive        Boolean         @default(true) @map("is_active")
  createdAt       DateTime        @default(now()) @map("created_at")
  updatedAt       DateTime        @updatedAt @map("updated_at")
  deletedAt       DateTime?       @map("deleted_at")
  deletedBy       String?         @map("deleted_by")

  tenant      Tenant      @relation(fields: [tenantId], references: [id])
  company     Company     @relation(fields: [companyId], references: [id])
  unit        Unit        @relation(fields: [unitId], references: [id])
  jobFunction JobFunction? @relation(fields: [jobFunctionId], references: [id])

  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([unitId])
  @@index([jobFunctionId])
  @@index([type, level, isActive])
  @@map("risks")
}

model Accident {
  id                    String           @id @default(uuid())
  tenantId              String           @map("tenant_id")
  companyId             String           @map("company_id")
  unitId                String           @map("unit_id")
  employeeId            String           @map("employee_id")
  jobFunctionId         String?          @map("job_function_id")
  code                  String           @unique
  regional              String?
  unitManagerName       String?          @map("unit_manager_name")
  salary                String?
  employeePhone         String?          @map("employee_phone")
  workSchedule          String?          @map("work_schedule")
  totalTimeInRole       String?          @map("total_time_in_role")
  activityType          AccidentActivityType? @map("activity_type")
  previousAccident      Boolean          @default(false) @map("previous_accident")
  previousAccidentDescription String?    @map("previous_accident_description")
  occurredAt            DateTime         @map("occurred_at")
  reportedAt            DateTime         @default(now()) @map("reported_at")
  location              String
  occurrenceAddress     String?          @map("occurrence_address")
  accidentType          AccidentType     @map("accident_type")
  typicalSubtypes       AccidentTypicalSubtype[] @default([]) @map("typical_subtypes")
  typicalSubtypeOther   String?          @map("typical_subtype_other")
  commuteSubtypes       AccidentCommuteSubtype[] @default([]) @map("commute_subtypes")
  commuteSubtypeOther   String?          @map("commute_subtype_other")
  workJourneyType       AccidentWorkJourneyType? @map("work_journey_type")
  scheduleChangeStart   String?          @map("schedule_change_start")
  scheduleChangeEnd     String?          @map("schedule_change_end")
  severity              AccidentSeverity
  status                AccidentStatus   @default(REPORTED)
  description           String
  injuredSide           AccidentInjuredSide? @map("injured_side")
  injuredBodyParts      AccidentBodyPart[] @default([]) @map("injured_body_parts")
  injuredBodyPartOther  String?          @map("injured_body_part_other")
  injuredBodyPart       String?          @map("injured_body_part")
  medicalCareProvided   Boolean          @default(false) @map("medical_care_provided")
  medicalCareTime       String?          @map("medical_care_time")
  leaveRequired         Boolean          @default(false) @map("leave_required")
  leaveDays             Int              @default(0) @map("leave_days")
  catIssued             Boolean          @default(false) @map("cat_issued")
  catNumber             String?          @map("cat_number")
  witnesses             String?
  immediateActions      String?          @map("immediate_actions")
  investigatorName      String?          @map("investigator_name")
  investigationStartedAt DateTime?       @map("investigation_started_at")
  immediateCause        String?          @map("immediate_cause")
  rootCause             String?          @map("root_cause")
  contributingFactors   String?          @map("contributing_factors")
  correctiveActions     String?          @map("corrective_actions")
  preventiveMeasures    String?          @map("preventive_measures")
  managerNotes          String?          @map("manager_notes")
  recommendations       String?
  conclusionSummary     String?          @map("conclusion_summary")
  closureDate           DateTime?        @map("closure_date")
  isActive              Boolean          @default(true) @map("is_active")
  createdAt             DateTime         @default(now()) @map("created_at")
  updatedAt             DateTime         @updatedAt @map("updated_at")
  deletedAt             DateTime?        @map("deleted_at")
  deletedBy             String?          @map("deleted_by")

  tenant      Tenant       @relation(fields: [tenantId], references: [id])
  company     Company      @relation(fields: [companyId], references: [id])
  unit        Unit         @relation(fields: [unitId], references: [id])
  employee    Employee     @relation(fields: [employeeId], references: [id])
  jobFunction JobFunction? @relation(fields: [jobFunctionId], references: [id])
  generatedDocuments AccidentGeneratedDocument[]
  evidences   AccidentEvidence[]

  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([unitId])
  @@index([employeeId])
  @@index([jobFunctionId])
  @@index([status, severity, occurredAt])
  @@map("accidents")
}

model FunctionTemplate {
  id            String    @id @default(uuid())
  tenantId      String    @map("tenant_id")
  jobFunctionId String    @map("function_id")
  documentType  String    @map("document_type")
  name          String
  filePath      String?   @map("file_path")
  fileContent   Bytes?    @map("file_content")
  version       Int
  variables     Json      @default("[]")
  isActive      Boolean   @default(true) @map("is_active")
  createdBy     String    @map("created_by")
  createdAt     DateTime  @default(now()) @map("created_at")
  deletedAt     DateTime? @map("deleted_at")
  deletedBy     String?   @map("deleted_by")

  tenant             Tenant              @relation(fields: [tenantId], references: [id])
  jobFunction        JobFunction         @relation(fields: [jobFunctionId], references: [id])
  creator            User                @relation("FunctionTemplateCreatedBy", fields: [createdBy], references: [id])
  generatedDocuments GeneratedDocument[]

  @@unique([jobFunctionId, documentType, version])
  @@index([tenantId])
  @@index([tenantId, jobFunctionId])
  @@index([jobFunctionId, documentType, isActive])
  @@map("function_templates")
}

model GeneratedDocument {
  id            String                  @id @default(uuid())
  tenantId      String                  @map("tenant_id")
  employeeId    String                  @map("employee_id")
  jobFunctionId String                  @map("function_id")
  unitId        String                  @map("unit_id")
  templateId    String                  @map("template_id")
  documentType  String                  @map("document_type")
  filePath      String?                 @map("file_path")
  fileContent   Bytes?                  @map("file_content")
  generatedBy   String                  @map("generated_by")
  generatedAt   DateTime                @default(now()) @map("generated_at")
  status        GeneratedDocumentStatus @default(ACTIVE)
  deletedAt     DateTime?               @map("deleted_at")
  deletedBy     String?                 @map("deleted_by")

  tenant      Tenant             @relation(fields: [tenantId], references: [id])
  employee    Employee           @relation(fields: [employeeId], references: [id])
  jobFunction JobFunction        @relation(fields: [jobFunctionId], references: [id])
  unit        Unit               @relation(fields: [unitId], references: [id])
  template    FunctionTemplate   @relation(fields: [templateId], references: [id])
  generator   User               @relation("GeneratedDocumentGeneratedBy", fields: [generatedBy], references: [id])
  auditLogs   DocumentAuditLog[]

  @@index([tenantId])
  @@index([employeeId, status])
  @@index([tenantId, employeeId])
  @@index([templateId])
  @@map("generated_documents")
}

model AccidentTemplate {
  id            String    @id @default(uuid())
  tenantId      String    @map("tenant_id")
  companyId     String    @map("company_id")
  documentType  String    @map("document_type")
  name          String
  filePath      String?   @map("file_path")
  fileContent   Bytes?    @map("file_content")
  version       Int
  variables     Json      @default("[]")
  isActive      Boolean   @default(true) @map("is_active")
  createdBy     String    @map("created_by")
  createdAt     DateTime  @default(now()) @map("created_at")
  deletedAt     DateTime? @map("deleted_at")
  deletedBy     String?   @map("deleted_by")

  tenant             Tenant                     @relation(fields: [tenantId], references: [id])
  company            Company                    @relation(fields: [companyId], references: [id])
  creator            User                       @relation(fields: [createdBy], references: [id])
  generatedDocuments AccidentGeneratedDocument[]

  @@unique([companyId, documentType, version])
  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([companyId, documentType, isActive])
  @@map("accident_templates")
}

model AccidentGeneratedDocument {
  id            String                  @id @default(uuid())
  tenantId      String                  @map("tenant_id")
  companyId     String                  @map("company_id")
  accidentId    String                  @map("accident_id")
  employeeId    String                  @map("employee_id")
  unitId        String                  @map("unit_id")
  templateId    String                  @map("template_id")
  documentType  String                  @map("document_type")
  filePath      String?                 @map("file_path")
  fileContent   Bytes?                  @map("file_content")
  generatedBy   String                  @map("generated_by")
  generatedAt   DateTime                @default(now()) @map("generated_at")
  status        GeneratedDocumentStatus @default(ACTIVE)
  deletedAt     DateTime?               @map("deleted_at")
  deletedBy     String?                 @map("deleted_by")

  tenant    Tenant           @relation(fields: [tenantId], references: [id])
  company   Company          @relation(fields: [companyId], references: [id])
  accident  Accident         @relation(fields: [accidentId], references: [id])
  employee  Employee         @relation(fields: [employeeId], references: [id])
  unit      Unit             @relation(fields: [unitId], references: [id])
  template  AccidentTemplate @relation(fields: [templateId], references: [id])
  generator User             @relation(fields: [generatedBy], references: [id])

  @@index([tenantId])
  @@index([companyId])
  @@index([accidentId, status])
  @@index([employeeId])
  @@index([templateId])
  @@map("accident_generated_documents")
}

model AccidentEvidence {
  id          String               @id @default(uuid())
  tenantId    String               @map("tenant_id")
  companyId   String               @map("company_id")
  accidentId  String               @map("accident_id")
  evidenceType AccidentEvidenceType @map("evidence_type")
  fileName    String               @map("file_name")
  mimeType    String               @map("mime_type")
  fileSize    Int                  @map("file_size")
  fileContent Bytes                @map("file_content")
  notes       String?              @map("notes")
  createdBy   String               @map("created_by")
  createdAt   DateTime             @default(now()) @map("created_at")
  deletedAt   DateTime?            @map("deleted_at")
  deletedBy   String?              @map("deleted_by")
  isActive    Boolean              @default(true) @map("is_active")

  tenant   Tenant   @relation(fields: [tenantId], references: [id])
  company  Company  @relation(fields: [companyId], references: [id])
  accident Accident @relation(fields: [accidentId], references: [id])
  creator  User     @relation("AccidentEvidenceCreatedBy", fields: [createdBy], references: [id])

  @@index([tenantId])
  @@index([companyId])
  @@index([accidentId, createdAt])
  @@index([evidenceType])
  @@map("accident_evidences")
}

model DocumentAuditLog {
  id         String              @id @default(uuid())
  tenantId   String              @map("tenant_id")
  documentId String              @map("document_id")
  action     DocumentAuditAction
  userId     String              @map("user_id")
  createdAt  DateTime            @default(now()) @map("created_at")

  tenant   Tenant            @relation(fields: [tenantId], references: [id])
  document GeneratedDocument @relation(fields: [documentId], references: [id])
  user     User              @relation("DocumentAuditUser", fields: [userId], references: [id])

  @@index([tenantId])
  @@index([documentId])
  @@index([userId])
  @@map("document_audit_logs")
}

model SstLegalDocument {
  id             String                 @id @default(uuid())
  tenantId       String                 @map("tenant_id")
  companyId      String                 @map("company_id")
  unitId         String?                @map("unit_id")
  documentType   SstLegalDocumentType   @map("document_type")
  title          String
  version        Int
  status         SstLegalDocumentStatus @default(ACTIVE)
  summary        String?
  contentHtml    String                 @map("content_html") @db.Text
  effectiveFrom  DateTime?              @map("effective_from") @db.Date
  effectiveUntil DateTime?              @map("effective_until") @db.Date
  generatedBy    String                 @map("generated_by")
  generatedAt    DateTime               @default(now()) @map("generated_at")
  deletedAt      DateTime?              @map("deleted_at")
  deletedBy      String?                @map("deleted_by")

  tenant    Tenant  @relation(fields: [tenantId], references: [id])
  company   Company @relation(fields: [companyId], references: [id])
  unit      Unit?   @relation(fields: [unitId], references: [id])
  generator User    @relation("SstLegalDocumentGeneratedBy", fields: [generatedBy], references: [id])

  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([companyId, documentType, version])
  @@index([unitId])
  @@index([status, generatedAt])
  @@map("sst_legal_documents")
}

model User {
  id           String    @id @default(uuid())
  tenantId     String?   @map("tenant_id")
  companyId    String?   @map("company_id")
  email        String    @unique
  passwordHash String    @map("password_hash")
  name         String
  role         Role      @default(CONSULTA)
  sessionVersion Int     @default(0) @map("session_version")
  avatarUrl    String?   @map("avatar_url")
  isActive     Boolean   @default(true) @map("is_active")
  lastLoginAt  DateTime? @map("last_login_at")
  createdBy    String?   @map("created_by")
  createdAt    DateTime  @default(now()) @map("created_at")
  updatedAt    DateTime  @updatedAt @map("updated_at")
  deletedAt    DateTime? @map("deleted_at")
  deletedBy    String?   @map("deleted_by")

  tenant                   Tenant?              @relation(fields: [tenantId], references: [id])
  company                  Company?             @relation("PrimaryCompany", fields: [companyId], references: [id])
  createdByUser            User?                @relation("UserCreatedBy", fields: [createdBy], references: [id])
  createdUsers             User[]               @relation("UserCreatedBy")
  createdFunctionTemplates FunctionTemplate[]   @relation("FunctionTemplateCreatedBy")
  generatedDocuments       GeneratedDocument[]  @relation("GeneratedDocumentGeneratedBy")
  createdAccidentTemplates AccidentTemplate[]
  generatedAccidentDocuments AccidentGeneratedDocument[]
  createdAccidentEvidences AccidentEvidence[]   @relation("AccidentEvidenceCreatedBy")
  generatedSstLegalDocuments SstLegalDocument[] @relation("SstLegalDocumentGeneratedBy")
  documentAuditLogs        DocumentAuditLog[]   @relation("DocumentAuditUser")
  companyAccess            UserCompanyAccess[]
  unitAccess               UserUnitAccess[]
  refreshTokens            RefreshToken[]
  loginVerificationCodes   LoginVerificationCode[]
  passwordResetTokens      PasswordResetToken[]
  notifications            Notification[]
  auditLogs                AuditLog[]

  @@index([tenantId])
  @@index([tenantId, deletedAt])
  @@index([companyId])
  @@map("users")
}

model Notification {
  id        String           @id @default(uuid())
  tenantId  String?          @map("tenant_id")
  companyId String?          @map("company_id")
  userId    String           @map("user_id")
  title     String
  message   String
  type      NotificationType @default(INFO)
  link      String?
  metadata  Json?
  isRead    Boolean          @default(false) @map("is_read")
  readAt    DateTime?        @map("read_at")
  isActive  Boolean          @default(true) @map("is_active")
  createdAt DateTime         @default(now()) @map("created_at")
  updatedAt DateTime         @updatedAt @map("updated_at")
  deletedAt DateTime?        @map("deleted_at")
  deletedBy String?          @map("deleted_by")

  tenant  Tenant?  @relation(fields: [tenantId], references: [id])
  company Company? @relation(fields: [companyId], references: [id])
  user    User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([userId, isRead])
  @@index([tenantId])
  @@index([companyId])
  @@index([createdAt])
  @@map("notifications")
}

model UserCompanyAccess {
  id        String   @id @default(uuid())
  userId    String   @map("user_id")
  companyId String   @map("company_id")
  createdAt DateTime @default(now()) @map("created_at")

  user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  company Company @relation(fields: [companyId], references: [id], onDelete: Cascade)

  @@unique([userId, companyId])
  @@index([companyId])
  @@map("user_company_access")
}

model UserUnitAccess {
  id        String   @id @default(uuid())
  userId    String   @map("user_id")
  unitId    String   @map("unit_id")
  createdAt DateTime @default(now()) @map("created_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  unit Unit @relation(fields: [unitId], references: [id], onDelete: Cascade)

  @@unique([userId, unitId])
  @@index([unitId])
  @@map("user_unit_access")
}

model RefreshToken {
  id        String    @id @default(uuid())
  userId    String    @map("user_id")
  tokenHash String    @unique @map("token_hash")
  expiresAt DateTime  @map("expires_at")
  revokedAt DateTime? @map("revoked_at")
  createdAt DateTime  @default(now()) @map("created_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([expiresAt])
  @@map("refresh_tokens")
}

model PasswordResetToken {
  id        String    @id @default(uuid())
  userId    String    @map("user_id")
  tokenHash String    @unique @map("token_hash")
  expiresAt DateTime  @map("expires_at")
  usedAt    DateTime? @map("used_at")
  ip        String?
  userAgent String?   @map("user_agent")
  createdAt DateTime  @default(now()) @map("created_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([expiresAt])
  @@map("password_reset_tokens")
}

model LoginVerificationCode {
  id        String    @id @default(uuid())
  userId    String    @map("user_id")
  codeHash  String    @map("code_hash")
  expiresAt DateTime  @map("expires_at")
  usedAt    DateTime? @map("used_at")
  ip        String?
  userAgent String?   @map("user_agent")
  createdAt DateTime  @default(now()) @map("created_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([expiresAt])
  @@map("login_verification_codes")
}

model EpiItem {
  id               String    @id @default(uuid())
  tenantId         String    @map("tenant_id")
  companyId        String    @map("company_id")
  name             String
  description      String?
  caNumber         String    @map("ca_number")
  caExpiry         DateTime? @map("ca_expiry") @db.Date
  manufacturer     String?
  unitOfMeasure    String    @default("unidade") @map("unit_of_measure")
  stockQuantity    Int       @default(0) @map("stock_quantity")
  minStockQuantity Int       @default(0) @map("min_stock_quantity")
  isActive         Boolean   @default(true) @map("is_active")
  createdAt        DateTime  @default(now()) @map("created_at")
  updatedAt        DateTime  @updatedAt @map("updated_at")
  deletedAt        DateTime? @map("deleted_at")
  deletedBy        String?   @map("deleted_by")

  tenant     Tenant        @relation(fields: [tenantId], references: [id])
  company    Company       @relation(fields: [companyId], references: [id])
  deliveries EpiDelivery[]

  @@index([tenantId])
  @@index([tenantId, companyId])
  @@map("epi_items")
}

model EpiDelivery {
  id               String            @id @default(uuid())
  tenantId         String            @map("tenant_id")
  companyId        String            @map("company_id")
  employeeId       String            @map("employee_id")
  epiItemId        String            @map("epi_item_id")
  quantity         Int               @default(1)
  deliveredAt      DateTime          @map("delivered_at") @db.Date
  reason           EpiDeliveryReason
  condition        EpiCondition      @default(NEW)
  deliveredBy      String?           @map("delivered_by")
  returnedAt       DateTime?         @map("returned_at") @db.Date
  notes            String?
  caNumberSnapshot String?           @map("ca_number_snapshot")
  isActive         Boolean           @default(true) @map("is_active")
  createdAt        DateTime          @default(now()) @map("created_at")
  updatedAt        DateTime          @updatedAt @map("updated_at")
  deletedAt        DateTime?         @map("deleted_at")
  deletedBy        String?           @map("deleted_by")

  tenant   Tenant   @relation(fields: [tenantId], references: [id])
  company  Company  @relation(fields: [companyId], references: [id])
  employee Employee @relation(fields: [employeeId], references: [id])
  epiItem  EpiItem  @relation(fields: [epiItemId], references: [id])

  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([employeeId])
  @@index([epiItemId])
  @@map("epi_deliveries")
}

model OccupationalExam {
  id            String                 @id @default(uuid())
  tenantId      String                 @map("tenant_id")
  companyId     String                 @map("company_id")
  unitId        String                 @map("unit_id")
  employeeId    String                 @map("employee_id")
  jobFunctionId String?                @map("job_function_id")
  examType      OccupationalExamType   @map("exam_type")
  name          String
  provider      String?
  performedAt   DateTime?              @map("performed_at") @db.Date
  dueDate       DateTime               @map("due_date") @db.Date
  result        OccupationalExamResult @default(PENDING)
  asoIssued     Boolean                @default(false) @map("aso_issued")
  asoNumber     String?                @map("aso_number")
  notes         String?
  isActive      Boolean                @default(true) @map("is_active")
  createdAt     DateTime               @default(now()) @map("created_at")
  updatedAt     DateTime               @updatedAt @map("updated_at")
  deletedAt     DateTime?              @map("deleted_at")
  deletedBy     String?                @map("deleted_by")

  tenant      Tenant       @relation(fields: [tenantId], references: [id])
  company     Company      @relation(fields: [companyId], references: [id])
  unit        Unit         @relation(fields: [unitId], references: [id])
  employee    Employee     @relation(fields: [employeeId], references: [id])
  jobFunction JobFunction? @relation(fields: [jobFunctionId], references: [id])

  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([unitId])
  @@index([employeeId])
  @@index([jobFunctionId])
  @@index([dueDate, result, isActive])
  @@map("occupational_exams")
}

model Training {
  id            String         @id @default(uuid())
  tenantId      String         @map("tenant_id")
  companyId     String         @map("company_id")
  unitId        String         @map("unit_id")
  employeeId    String         @map("employee_id")
  jobFunctionId String?        @map("job_function_id")
  name          String
  provider      String?
  workloadHours Int?           @map("workload_hours")
  completedAt   DateTime?      @map("completed_at") @db.Date
  dueDate       DateTime        @map("due_date") @db.Date
  certificateUrl String?       @map("certificate_url")
  status        TrainingStatus @default(SCHEDULED)
  notes         String?
  isActive      Boolean        @default(true) @map("is_active")
  createdAt     DateTime       @default(now()) @map("created_at")
  updatedAt     DateTime       @updatedAt @map("updated_at")
  deletedAt     DateTime?      @map("deleted_at")
  deletedBy     String?        @map("deleted_by")

  tenant      Tenant       @relation(fields: [tenantId], references: [id])
  company     Company      @relation(fields: [companyId], references: [id])
  unit        Unit         @relation(fields: [unitId], references: [id])
  employee    Employee     @relation(fields: [employeeId], references: [id])
  jobFunction JobFunction? @relation(fields: [jobFunctionId], references: [id])

  @@index([tenantId])
  @@index([tenantId, companyId])
  @@index([unitId])
  @@index([employeeId])
  @@index([jobFunctionId])
  @@index([dueDate, status, isActive])
  @@map("trainings")
}

model AuditLog {
  id          String      @id @default(uuid())
  tenantId    String?     @map("tenant_id")
  actorUserId String?     @map("actor_user_id")
  action      AuditAction
  entityType  String      @map("entity_type")
  entityId    String?     @map("entity_id")
  metadata    Json?
  ip          String?
  userAgent   String?     @map("user_agent")
  createdAt   DateTime    @default(now()) @map("created_at")

  tenant Tenant? @relation(fields: [tenantId], references: [id])
  actor  User?   @relation(fields: [actorUserId], references: [id])

  @@index([tenantId])
  @@index([entityType, entityId])
  @@index([actorUserId])
  @@map("audit_logs")
}
