{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @moby/shared build && pnpm --filter frontend build",
  "outputDirectory": "apps/frontend/.next",
  "framework": "nextjs"
}
